export default () => {
    return {
        setSearch(state, search) {
            state.search = search
        },
        setDetail(state, detail) {
            state.detail = detail
        }
    }
}
